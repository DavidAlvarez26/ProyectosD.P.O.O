package persistencia;

import java.util.List;

import logica.Tiquete;

public interface IPersistenciaTiquetes {
    List<Tiquete> cargarTiquetes(String archivo);
    void guardarTiquetes(String archivo, List<Tiquete> tiquetes);
}
