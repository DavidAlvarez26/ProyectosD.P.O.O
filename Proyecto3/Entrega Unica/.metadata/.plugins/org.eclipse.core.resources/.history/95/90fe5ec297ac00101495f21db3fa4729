package tests;

import java.sql.Date;
import java.util.List;

import logica.BoletaMaster;
import logica.Evento;

public class TestConsultaCatalogo {
    public static void main(String[] args) {
        BoletaMaster sistema = new BoletaMaster(null, null, null, null, null, null);
        sistema.cargarDatos();

        List<Evento> catalogo = sistema.consultarCatalogoEventos("Concierto", Date.valueOf("2025-11-01"), "Bogotá");

        System.out.println("📅 Catálogo de eventos:");
        for (Evento e : catalogo) {
            System.out.println("Evento: " + e.getNombre() +
                               " | Tipo: " + e.getTipo() +
                               " | Fecha: " + e.getFecha() +
                               " | Lugar: " + e.getVenue().getNombre());
        }
    }
}